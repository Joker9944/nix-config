/**
  Addresses of the nyx cluster nodes, keyed by host name. Data rather than a
  helper, because the firewall rules on every node need the whole set and not
  just that node's own entry.
*/
_: {
  tars = "192.168.0.21";
  case = "192.168.0.22";
  kipp = "192.168.0.23";
  mother = "192.168.0.24";
}
