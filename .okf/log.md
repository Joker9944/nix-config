# Update Log

## 2026-07-19

* **Creation**: New concept [architecture/module-layout](/architecture/module-layout.md) — captures the folder + `files/` conventions for any nix module in the repo (not just mixins: home-manager mixins, NixOS mixins, and flake-level `modules/nixos/` and `modules/home/` all follow the same rule). Cross-linked from [mixin-pattern](/architecture/mixin-pattern.md), [workflows/add-mixin](/workflows/add-mixin.md), and the [architecture index](/architecture/index.md). Initial draft lived inside `mixin-pattern.md`; extracted to its own concept after the user pointed out the scope was broader than mixins.
* **Note**: `users/mixins/programs/vscode/openssh-no-checkperm.patch` predates the `files/` rule and sits at the module root — a pre-existing inconsistency, flagged here for future cleanup.

## 2026-07-17

* **Creation**: New decision [release-policy](/decisions/release-policy.md) — captures the "stable nixos, matching home-manager, hyprland from upstream" choice with its trade-offs. Extracted from the release-policy paragraph that was previously in [overview](/overview.md), which is a decision, not a definitional fact about the repo.
* **Update**: Trimmed [overview](/overview.md) further — dropped the Hosts section (dupe of `index.md`'s progressive disclosure) and the release-policy paragraph (now a decision). Overview is now just the mental-model paragraph: mixin pattern + standalone home-manager.
* **Review pass**: Walked every concept file with the "cut duplication / cut fluff / keep load-bearing" lens. Both existing decision files ([standalone-home-manager](/decisions/standalone-home-manager.md), [enable-flag-mixins](/decisions/enable-flag-mixins.md)) held up as-is. All indexes untouched. See prior entries in this session for the specific concept trims.
* **Update**: Trimmed three workflow playbooks. [add-mixin](/workflows/add-mixin.md) dropped its inline mixin template (byte-identical to `mixin-pattern.md#shape`) and now points there. [lookup-hm-option](/workflows/lookup-hm-option.md) dropped the "Known 26.05 hallucination traps" table (mirrored the skill's own table on a rot schedule) and softened the version-specific mention. [formatting-and-cspell](/workflows/formatting-and-cspell.md) replaced the concrete "already whitelisted" word list with a description of the categories, since the list drifts.
* **Update**: Trimmed both host concepts ([HAL9000](/hosts/HAL9000.md), [wintermute](/hosts/wintermute.md)) — dropped the verbatim host-record code block and the "Enabled mixin categories" bullet lists (both duplicated files). Kept load-bearing hardware facts (monitor layout, panel, ThinkPad model) and the host-specific quirks that aren't obvious from reading `mixins.nix`.
* **Update**: Trimmed [architecture/custom-lib](/architecture/custom-lib.md) — folded trivial utility helpers (indent, mkCommand, first, last, ls, …) into a single "self-descriptive names" line rather than dedicating table rows to each. Kept helpers with non-obvious use and real-caller pointers.
* **Update**: Trimmed [architecture/entry-points](/architecture/entry-points.md) — replaced the verbatim `flake.nix#nixosConfigurations` code block with prose that captures the non-obvious mechanics (hostname key, `<user>@<host>` key, first-arg passing), and dropped the "currently just `default`" enumerations for `nixosModules`/`homeModules`.
* **Update**: Tightened [architecture/auto-discovery](/architecture/auto-discovery.md) — merged the "what it does" / "where it's used" sections, and pinned the `importApply` variant to its three actual callers instead of describing it abstractly.
* **Update**: Trimmed [architecture/mixin-pattern](/architecture/mixin-pattern.md) — dropped the synthetic enable-idiom example (real code in `hosts/HAL9000/mixins.nix` and `users/joker9944/config/mixins.nix` teaches it better) and the "30 lines, mkEnableOption + lib.mkIf" flavor line (duplicates the shape template above it).
* **Update**: Trimmed [overview](/overview.md) — replaced pinned toolchain versions with a release policy statement and reduced the hosts section to a bare link. Version numbers rot on a fixed schedule and belong in `flake.nix`, not here.
* **Initialization**: Bootstrapped the OKF bundle with overview, architecture concepts (mixin pattern, auto-discovery, entry points, custom lib), host concepts (HAL9000, wintermute), workflow playbooks (rebuild, add-mixin, lookup-hm-option, secrets, formatting-and-cspell), and decision records (standalone-home-manager, enable-flag-mixins).
